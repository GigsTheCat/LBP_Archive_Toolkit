<Window x:Class="LbpArchiveToolkit.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Extracting..." Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" Topmost="True"
        FontFamily="Segoe UI" WindowStyle="None" BorderThickness="0" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" GlassFrameThickness="-1" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimary" Color="#19132B"/> 
        <SolidColorBrush x:Key="BgCard" Color="#2D2447"/>
        <SolidColorBrush x:Key="BgCardDarker" Color="#221A35"/>
        <SolidColorBrush x:Key="FgPrimary" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="FgSecondary" Color="#B7AEDB"/>
        <SolidColorBrush x:Key="BorderSubtle" Color="#453868"/>
        
        <SolidColorBrush x:Key="LbpOrange" Color="#FFB703"/>
        <SolidColorBrush x:Key="LbpPink" Color="#F72585"/>
        <SolidColorBrush x:Key="LbpCyan" Color="#4CC9F0"/>
        <SolidColorBrush x:Key="LbpEmerald" Color="#06D6A0"/>
        
        <Style x:Key="TitleBarCloseBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource FgSecondary}"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,14,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PopitCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.3" Direction="270" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BubbleButton" TargetType="Button">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="20"> 
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#3D325C"/>
                                <Setter Property="Foreground" Value="#6A5C8C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnCancel" TargetType="Button" BasedOn="{StaticResource BubbleButton}">
            <Setter Property="Background" Value="{StaticResource LbpPink}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4A9F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="14" BorderThickness="1" BorderBrush="{StaticResource BorderSubtle}">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#130F25" Offset="0.0" />
                <GradientStop Color="#2A2045" Offset="1.0" />
            </LinearGradientBrush>
        </Border.Background>

        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="32" Background="Transparent">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="{StaticResource FgSecondary}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="✕" Style="{StaticResource TitleBarCloseBtn}" Click="TitleBar_Close_Click"/>
                </StackPanel>
            </Grid>

            <Border Style="{StaticResource PopitCard}" Margin="20,10,20,25" Padding="25">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,20" MinHeight="40">
                        <TextBlock x:Name="txtStatus" Text="Preparing extraction..." 
                                   Foreground="{StaticResource FgPrimary}" FontSize="14" FontWeight="SemiBold"
                                   TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,4"/>
                                   
                        <!-- This is the new tiny sub-status text -->
                        <TextBlock x:Name="txtSubStatus" Text="Initializing..." 
                                   Foreground="{StaticResource FgSecondary}" FontSize="11" FontWeight="Normal"
                                   TextWrapping="Wrap" TextAlignment="Center"/>
                    </StackPanel>

                    <Border Grid.Row="1" Background="{StaticResource BgCardDarker}" CornerRadius="9" Height="18" Margin="0,0,0,25" ClipToBounds="True">
                        <ProgressBar x:Name="pbProgress" Background="Transparent" Foreground="{StaticResource LbpEmerald}" BorderThickness="0"/>
                    </Border>

                    <Button x:Name="btnCancel" Grid.Row="2" Content="CANCEL" Width="120"
                            HorizontalAlignment="Center" Style="{StaticResource BtnCancel}" Click="BtnCancel_Click"/>
                </Grid>
            </Border>
        </DockPanel>
    </Border>
</Window>